import React from 'react';
import { render, fireEvent, act } from '@testing-library/react';

import Facets from './index';

test('renders without crashing', async () => {
  test.todo('placeholder');
});

test('shows facets by project', async () => {
  test.todo('placeholder');
});

test('displays error when facets api offline', async () => {
  test.todo('placeholder');
});
test('shows applied constraints', async () => {
  test.todo('placeholder');
});

test('renders results in <Search /> on facets form submit', async () => {
  test.todo('placeholder');
});
