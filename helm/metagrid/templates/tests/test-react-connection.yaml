apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metagrid.fullname" . }}-test-connection"
  labels:
    {{- include "metagrid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metagrid.fullname" . }}:{{ .Values.react.service.port }}']
  restartPolicy: Never
