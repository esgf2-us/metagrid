suite: test node_status serviceaccount
templates:
  - templates/node_status/serviceaccount.yaml
release:
  name: metagrid
  namespace: production
chart:
  version: v1.0.0
  appVersion: v1.0.0
set:
  django:
    secretKey: abcd1234 # need static for checksum
  nodeStatusBackend:
    enabled: true
tests:
  - it: should create service default account
    template: templates/node_status/serviceaccount.yaml
    asserts:
      - matchSnapshot: {}
