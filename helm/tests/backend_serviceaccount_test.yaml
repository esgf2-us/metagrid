suite: test backend serviceaccount
templates:
  - templates/backend/serviceaccount.yaml
release:
  name: metagrid
  namespace: production
chart:
  version: v1.0.0
  appVersion: v1.0.0
set:
  django:
    secretKey: abcd1234 # need static for checksum
tests:
  - it: should create service default account
    template: templates/backend/serviceaccount.yaml
    asserts:
      - matchSnapshot: {}
