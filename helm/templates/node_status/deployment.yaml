{{- if .Values.nodeStatusBackend.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "metagrid.fullname" . }}-node-status-backend
  labels:
    app.kubernetes.io/component: node-status-backend
    {{- include "metagrid.labels" . | nindent 4 }}
spec:
  {{- if not .Values.nodeStatusBackend.autoscaling.enabled }}
  replicas: {{ .Values.nodeStatusBackend.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/component: node-status-backend
      {{- include "metagrid.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/node_status/configmap.yaml") . | sha256sum }}
      {{- with .Values.nodeStatusBackend.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        app.kubernetes.io/component: node-status-backend
        {{- include "metagrid.labels" . | nindent 8 }}
        {{- with .Values.nodeStatusBackend.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.nodeStatusBackend.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "metagrid.fullname" . }}-node-status-backend
      securityContext:
        {{- toYaml .Values.nodeStatusBackend.podSecurityContext | nindent 8 }}
      containers:
      - name: prometheus
        securityContext:
          {{- toYaml .Values.nodeStatusBackend.prometheus.securityContext | nindent 10 }}
        image: "{{ .Values.nodeStatusBackend.prometheus.image.repository }}:{{ .Values.nodeStatusBackend.prometheus.image.tag }}"
        imagePullPolicy: {{ .Values.nodeStatusBackend.prometheus.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.nodeStatusBackend.service.port }}
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /-/healthy
            port: http
            scheme: HTTP
          timeoutSeconds: {{ .Values.nodeStatusBackend.prometheus.livenessProbe.timeoutSeconds }}
          periodSeconds: {{ .Values.nodeStatusBackend.prometheus.livenessProbe.periodSeconds }}
          failureThreshold: {{ .Values.nodeStatusBackend.prometheus.livenessProbe.failureThreshold }}
        readinessProbe:
          httpGet:
            path: /-/ready
            port: http
            scheme: HTTP
          timeoutSeconds: {{ .Values.nodeStatusBackend.prometheus.readinessProbe.timeoutSeconds }}
          periodSeconds: {{ .Values.nodeStatusBackend.prometheus.readinessProbe.periodSeconds }}
          failureThreshold: {{ .Values.nodeStatusBackend.prometheus.readinessProbe.failureThreshold }}
        resources:
          {{- toYaml .Values.nodeStatusBackend.prometheus.resources | nindent 10 }}
        volumeMounts:
        - name: config-volume
          mountPath: /etc/prometheus/prometheus.yml
          subPath: prometheus.yml
        - name: config-volume
          mountPath: /etc/prometheus/esgf_node_status_targets.yml
          subPath: esgf_node_status_targets.yml
        - name: prom-data
          mountPath: /prometheus
        {{- with .Values.nodeStatusBackend.prometheus.volumeMounts }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      - name: blackbox
        securityContext:
          {{- toYaml .Values.nodeStatusBackend.blackbox.securityContext | nindent 10 }}
        image: "{{ .Values.nodeStatusBackend.blackbox.image.repository }}:{{ .Values.nodeStatusBackend.blackbox.image.tag }}"
        imagePullPolicy: {{ .Values.nodeStatusBackend.blackbox.image.pullPolicy }}
        ports:
        - name: blackbox-http
          containerPort: 9115
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /-/healthy
            port: blackbox-http
          timeoutSeconds: {{ .Values.nodeStatusBackend.blackbox.livenessProbe.timeoutSeconds }}
          periodSeconds: {{ .Values.nodeStatusBackend.blackbox.livenessProbe.periodSeconds }}
          failureThreshold: {{ .Values.nodeStatusBackend.blackbox.livenessProbe.failureThreshold }}
        readinessProbe:
          httpGet:
            path: /-/healthy
            port: blackbox-http
          timeoutSeconds: {{ .Values.nodeStatusBackend.blackbox.readinessProbe.timeoutSeconds }}
          periodSeconds: {{ .Values.nodeStatusBackend.blackbox.readinessProbe.periodSeconds }}
          failureThreshold: {{ .Values.nodeStatusBackend.blackbox.readinessProbe.failureThreshold }}
        resources:
          {{- toYaml .Values.nodeStatusBackend.blackbox.resources | nindent 10 }}
        {{- with .Values.nodeStatusBackend.blackbox.volumeMounts }}
        volumeMounts:
          {{- toYaml . | nindent 8 }}
        {{- end }}
      volumes:
      - name: config-volume
        configMap:
          name: {{ include "metagrid.fullname" . }}-node-status-backend-config
      - name: prom-data
        emptyDir: {}
      {{- with .Values.nodeStatusBackend.volumes }}
        {{- toYaml . | nindent 6 }}
      {{- end }}
      {{- with .Values.nodeStatusBackend.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeStatusBackend.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeStatusBackend.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
